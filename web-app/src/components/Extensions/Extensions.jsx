import React from 'react'

export default function Extensions({modalType}) {
    return (
        <div className={`d-${modalType === "Extensions" ? 'flex' : 'none'}`}>
            asdfasdf
        </div>
    )
}
